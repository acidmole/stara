<!DOCTYPE html>
<html>
  <head>
    <title>Stara - Sarjataulukkkorakennin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">Stara - Sarjataulukkkorakennin</a>
    
      <div class="container-fluid">
        <ul class="navbar-nav mr-auto" >
          <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" >Sarjat</a>
              <ul class="dropdown-menu">
                <% @competitions.each do |competition| %>
                <li><%= link_to competition.name, games_path(competition_id: competition.id), class: "dropdown-item" %></li>
                <% end %>
              </ul>
            </li>
        </ul>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>
